**********
Quickstart
**********

- Copy the ``cobbler.yaml`` file from ``config/cobbler.yaml`` to ``$HOME/.cobbler.yaml`` and adjust the values to your
  needs
- Build the project using the makefile
